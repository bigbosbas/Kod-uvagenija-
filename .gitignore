node_modules/
.env
.env.local

# Сам продукт (PDF) не должен попадать в git — платный контент, живёт только в S3.
*.pdf

# Исходники дизайн-пакета — рабочие материалы, не часть деплоя.
_extracted/
*.zip
Prototype design.png
